export function answerPath(part) {
  return `answer-${part}.md`;
}

export function attemptsPath(part) {
  return `attempts-${part}`;
}

export function inputPath() {
  return "input.txt";
}

export function problemPath(part) {
  return `problem-${part}.md`;
}

export function outputPath(part) {
  return `output-${part}.txt`;
}
